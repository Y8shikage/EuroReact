# 🔧 Конфигурация бэкенда

Этот файл содержит настройки для подключения к Python-серверу валидации видео.

## 📋 Параметры

### `BACKEND_URL`
URL Python-сервера через ngrok туннель.

**Важно:** Этот URL нужно обновлять каждый раз при перезапуске ngrok, так как ngrok генерирует новый URL при каждом запуске (если не используется платная подписка с фиксированным URL).

### `ENDPOINTS`
API endpoints на Python-сервере:
- `VALIDATE_VIDEO`: `/api/validate-video` - проверка codec и fps
- `HEALTH`: `/health` - проверка работы сервера
- `REQUIREMENTS`: `/api/requirements` - получение требований

### `TIMEOUT`
Таймаут запроса в миллисекундах (по умолчанию 30 секунд).

### `ENABLE_BACKEND_VALIDATION`
Включить/выключить отправку видео на Python-сервер:
- `true` - видео отправляется на сервер для проверки codec и fps
- `false` - проверка codec и fps пропускается (локальная валидация)

## 🚀 Использование

### При работе с Python-сервером:
```json
{
  "BACKEND_URL": "https://your-ngrok-url.ngrok-free.dev",
  "ENABLE_BACKEND_VALIDATION": true
}
```

### При работе без Python-сервера (только локальная проверка):
```json
{
  "BACKEND_URL": "",
  "ENABLE_BACKEND_VALIDATION": false
}
```

## ⚙️ Обновление ngrok URL

Когда Python-сервер перезапущен с новым ngrok URL:

1. Откройте `src/config/backend.json`
2. Обновите поле `BACKEND_URL`
3. Сохраните файл
4. Перезапустите React приложение (или подождите hot-reload)

## 🔗 Связь с Python-сервером

Python-сервер должен быть запущен на другом ПК:

```bash
cd pythonserver
uvicorn server:app --host 0.0.0.0 --port 8000 --reload
```

И ngrok туннель:

```bash
ngrok http 8000
```

Скопируйте URL из ngrok (например, `https://abc-def.ngrok-free.dev`) и вставьте в `BACKEND_URL`.

