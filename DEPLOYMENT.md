# 🚀 Инструкция по развёртыванию системы

## 📦 Архитектура системы

Система состоит из **двух независимых частей**:

1. **React фронтенд** (этот проект) - веб-интерфейс для пользователей
2. **Python бэкенд** (папка `pythonserver`) - сервер валидации codec и fps

**Важно:** Папка `pythonserver` должна быть перенесена на **отдельный ПК** и запущена там.

---

## 🖥️ ПК 1: Python-сервер (бэкенд)

### 1. Скопируйте папку `pythonserver` на целевой ПК

### 2. Установите зависимости:

```bash
cd pythonserver
pip install -r requirements.txt
```

### 3. Установите FFmpeg:

**Windows:**
```bash
winget install ffmpeg
```

**Linux:**
```bash
sudo apt install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

### 4. Запустите Python-сервер:

```bash
uvicorn server:app --host 0.0.0.0 --port 8000 --reload
```

Сервер будет доступен на `http://localhost:8000`

### 5. Запустите ngrok туннель (в отдельном терминале):

```bash
ngrok http 8000
```

**Важно:** Сохраните URL, который выдаст ngrok (например, `https://abc-def.ngrok-free.dev`)

---

## 💻 ПК 2: React фронтенд

### 1. Настройте подключение к Python-серверу:

Откройте `src/config/backend.json` и обновите URL:

```json
{
  "BACKEND_URL": "https://abc-def.ngrok-free.dev",
  "ENABLE_BACKEND_VALIDATION": true
}
```

Вставьте сюда URL из ngrok (без слеша в конце).

### 2. Установите зависимости (если ещё не установлены):

```bash
npm install
```

### 3. Запустите React приложение:

```bash
npm start
```

Приложение откроется на `http://localhost:3000`

---

## 🔄 Работа системы

### Что проверяется на фронтенде (JavaScript):
✅ Длительность видео (15 или 30 сек)  
✅ Разрешение (сравнение с 34 экранами)  
✅ Контейнер (.mp4)  
✅ Размер файла (до 400 МБ)

### Что проверяется на бэкенде (Python):
✅ Codec (должен быть h264)  
✅ FPS (должен быть 25 ±0.1)

### Процесс проверки:

1. Пользователь загружает видео на сайт
2. Фронтенд проверяет: duration, resolution, container, size
3. Фронтенд отправляет видео через ngrok на Python-сервер
4. Python-сервер анализирует: codec, fps
5. Python-сервер возвращает результат в JSON
6. Фронтенд объединяет все результаты и показывает пользователю

---

## ⚠️ Важные моменты

### 🔄 Обновление ngrok URL

**Проблема:** При каждом перезапуске ngrok генерирует новый URL.

**Решение:**
1. Скопируйте новый URL из ngrok
2. Обновите `src/config/backend.json`
3. Перезапустите React приложение

**Альтернатива:** Используйте платную подписку ngrok с фиксированным URL.

### 📁 Синхронизация файлов

**Файлы, которые должны быть одинаковы:**
- `src/data/screenRequirements.json` (фронтенд)
- `pythonserver/screenRequirements.json` (бэкенд)

При изменении требований обновите оба файла!

### 🔒 Лимиты размера файла

**Важно:** Лимиты синхронизированы между фронтендом и бэкендом:
- Фронтенд: 400 МБ (`commonSettings.maxSize` в `screenRequirements.json`)
- Бэкенд: 400 МБ (`MAX_FILE_SIZE` в `server.py`)

Если нужно изменить лимит, обновите оба значения!

---

## 🧪 Проверка работоспособности

### 1. Проверьте Python-сервер:

```bash
curl "http://localhost:8000/health"
```

Должно вернуть:
```json
{"status": "ok", "message": "Video validator server is running"}
```

### 2. Проверьте ngrok туннель:

```bash
curl -H "ngrok-skip-browser-warning: true" "https://your-ngrok-url/health"
```

### 3. Проверьте фронтенд:

Откройте `http://localhost:3000` и загрузите тестовое видео.

---

## 🐛 Troubleshooting

### Ошибка: "Не удалось проверить codec и fps на сервере"

**Причины:**
1. Python-сервер не запущен
2. Неверный URL в `backend.json`
3. ngrok туннель закрыт
4. Проблемы с сетью

**Решение:**
1. Проверьте, что Python-сервер запущен: `curl http://localhost:8000/health`
2. Проверьте, что ngrok запущен
3. Обновите URL в `backend.json`

### Python-сервер падает при загрузке видео

**Причины:**
1. Не установлен FFmpeg
2. Видео повреждено
3. Недостаточно места на диске

**Решение:**
1. Установите FFmpeg: `ffmpeg -version`
2. Проверьте логи Python-сервера
3. Проверьте место на диске

### Фронтенд не отправляет видео на сервер

**Проверьте:**
1. `ENABLE_BACKEND_VALIDATION: true` в `backend.json`
2. URL заполнен корректно
3. Откройте DevTools → Network и проверьте запросы

---

## 📞 Контакты

При возникновении проблем проверьте:
1. Логи Python-сервера (в терминале где запущен uvicorn)
2. Логи ngrok (в терминале где запущен ngrok)
3. DevTools → Console в браузере

---

**Версия:** 1.0.0  
**Дата:** Ноябрь 2024

