# Справка по авторизации

## 🔐 Тестовые учетные данные

### Администратор
- **Логин:** `Admin` (регистр не важен: можно `admin`, `ADMIN`, `AdMiN`)
- **Пароль:** `admin148` (регистр важен!)
- **Роль:** Администратор системы
- **Доступ:** Все функции, включая административную панель

### Пользователь
- **Логин:** `Eurouser` (регистр не важен: можно `eurouser`, `EUROUSER`, `EuroUser`)
- **Пароль:** `user148` (регистр важен!)
- **Роль:** Обычный пользователь
- **Доступ:** Загрузка видео, просмотр истории, уведомления

---

## 🚀 Быстрый вход

На странице авторизации есть две кнопки для быстрого входа:

1. **Кнопка "Админ"** - автоматически заполняет поля для входа администратора
2. **Кнопка "Пользователь"** - автоматически заполняет поля для входа пользователя

После нажатия на кнопку просто нажмите "Войти".

---

## 🔧 Решение проблем

### Проблема: "Неверный логин или пароль"

**Решение 1: Проверьте данные**
- Убедитесь, что пароль введен правильно (регистр важен!)
- Для администратора: пароль `admin148`, а НЕ `Admin148`
- Для пользователя: пароль `user148`, а НЕ `User148`

**Решение 2: Используйте кнопки быстрого входа**
- Нажмите кнопку "Админ" или "Пользователь"
- Затем нажмите "Войти"

**Решение 3: Сброс кэша**
1. Нажмите кнопку "Сбросить кэш" внизу формы
2. Дождитесь уведомления
3. Попробуйте войти снова

**Решение 4: Полная очистка браузера**
1. Откройте консоль разработчика (F12)
2. Выполните команду:
   ```javascript
   localStorage.clear();
   location.reload();
   ```
3. Попробуйте войти снова

---

## 🔍 Отладка (для разработчиков)

### Проверка данных в localStorage

Откройте консоль браузера (F12) и выполните:

```javascript
// Посмотреть всех пользователей
console.log(JSON.parse(localStorage.getItem('users')));

// Посмотреть текущего авторизованного пользователя
console.log(JSON.parse(localStorage.getItem('user')));
```

### Логи при входе

При попытке входа в консоли появятся следующие логи:

1. `Попытка входа: {username: "...", password: "..."}` - ваши введенные данные
2. `Доступные пользователи: [...]` - список всех пользователей в системе
3. `Найденный пользователь: {...}` - найденный пользователь (если успешно)
4. `Пользователь успешно авторизован` - успешная авторизация

Если пользователь не найден, проверьте:
- Правильность ввода пароля (регистр важен)
- Наличие пользователей в localStorage

### Ручной сброс данных

```javascript
// Удалить только пользователей
localStorage.removeItem('users');

// Удалить текущую сессию
localStorage.removeItem('user');

// Перезагрузить страницу
location.reload();
```

---

## 📝 Примеры правильного ввода

### ✅ Правильно (Администратор)
- Логин: `Admin` / `admin` / `ADMIN` / `AdMiN`
- Пароль: `admin148`

### ❌ Неправильно (Администратор)
- Логин: ` Admin` (с пробелом впереди)
- Пароль: `Admin148` (неверный регистр)
- Пароль: `admin 148` (пробел в пароле)

### ✅ Правильно (Пользователь)
- Логин: `Eurouser` / `eurouser` / `EUROUSER`
- Пароль: `user148`

### ❌ Неправильно (Пользователь)
- Логин: `Euro user` (пробел)
- Пароль: `User148` (неверный регистр)
- Пароль: `user 148` (пробел в пароле)

---

## 💡 Дополнительные возможности

### Показать/скрыть пароль

Рядом с полем пароля есть кнопка с иконкой глаза (👁️):
- Нажмите на нее, чтобы увидеть введенный пароль
- Нажмите еще раз, чтобы скрыть

Это помогает убедиться, что пароль введен правильно.

---

## 🆘 Все еще не работает?

Если ничего из вышеперечисленного не помогло:

1. **Проверьте, что сервер запущен:**
   - Откройте http://localhost:3000
   - Должна загрузиться страница авторизации

2. **Проверьте консоль на ошибки:**
   - Откройте F12
   - Перейдите на вкладку Console
   - Ищите красные ошибки

3. **Попробуйте другой браузер:**
   - Chrome, Firefox, Edge, Safari

4. **Очистите кэш браузера:**
   - Ctrl + Shift + Delete
   - Очистить кэш и cookies

5. **Перезапустите сервер:**
   - Остановите сервер (Ctrl + C)
   - Запустите снова: `npm start`

