# 🎬 Video Validator Server

Серверная часть системы валидации видеофайлов для цифровых экранов.  
FastAPI сервер для проверки технических характеристик видео (codec и fps).

---

## 📋 Описание

Этот сервер принимает видеофайлы через POST запрос и проверяет их соответствие техническим требованиям:
- **Codec**: должен быть `h264` (поддерживаются варианты: h264, libx264, avc1)
- **FPS**: должен быть `25` кадров в секунду (допустимая погрешность ±0.1)

Сервер работает в связке с React фронтендом через ngrok туннель и возвращает результаты валидации в JSON формате.

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

#### Установка Python зависимостей

```bash
pip install -r requirements.txt
```

#### Установка FFmpeg (системная утилита)

FFmpeg необходим для анализа видеофайлов. Установите его в зависимости от вашей операционной системы:

**Windows:**
```bash
# Через winget (Windows 10+)
winget install ffmpeg

# Или скачайте с официального сайта
# https://ffmpeg.org/download.html
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

**Проверка установки:**
```bash
ffmpeg -version
ffprobe -version
```

### 2. Запуск сервера локально

```bash
uvicorn server:app --host 0.0.0.0 --port 8000 --reload
```

Сервер будет доступен по адресу: `http://localhost:8000`

### 3. Настройка ngrok туннеля

В отдельном терминале запустите ngrok:

```bash
ngrok http 8000
```

Сервер будет доступен через ngrok URL: `https://landless-serpentinely-amari.ngrok-free.dev/`

---

## 📁 Структура проекта

```
pythonserver/
├── server.py                    # Основной файл FastAPI сервера
├── screenRequirements.json      # Конфигурация требований к видео
├── requirements.txt             # Python зависимости
├── README.md                    # Документация (этот файл)
└── .gitignore                   # Игнорируемые файлы
```

---

## 🔌 API Endpoints

### 1. **POST** `/api/validate-video`

Валидация видеофайла.

**Параметры:**
- `file` (multipart/form-data) - видеофайл (обязательный)
- `screen_id` (query parameter) - ID экрана (опциональный, не используется на бэкенде)

**Пример запроса (curl):**
```bash
curl -X POST "http://localhost:8000/api/validate-video" \
  -F "file=@test_video.mp4"
```

**Пример запроса через ngrok:**
```bash
curl -X POST "https://landless-serpentinely-amari.ngrok-free.dev/api/validate-video" \
  -H "ngrok-skip-browser-warning: true" \
  -F "file=@test_video.mp4"
```

**Ответ при успехе:**
```json
{
  "valid": true,
  "codec": "h264",
  "fps": 25.0,
  "errors": []
}
```

**Ответ при ошибках:**
```json
{
  "valid": false,
  "codec": "mpeg4",
  "fps": 30.0,
  "errors": [
    "Неверный кодек: mpeg4, требуется h264",
    "Неверный FPS: 30.0, требуется 25"
  ]
}
```

### 2. **GET** `/health`

Проверка здоровья сервера.

**Пример запроса:**
```bash
curl "http://localhost:8000/health"
```

**Ответ:**
```json
{
  "status": "ok",
  "message": "Video validator server is running"
}
```

### 3. **GET** `/api/requirements`

Получение текущих требований к видео (для отладки).

**Пример запроса:**
```bash
curl "http://localhost:8000/api/requirements"
```

**Ответ:**
```json
{
  "commonSettings": {
    "fps": 25,
    "container": "mp4",
    "codec": "h264",
    "maxSize": 400,
    "allowedDurations": [15, 30]
  },
  "screens": [...]
}
```

---

## ⚙️ Конфигурация

### Файл `screenRequirements.json`

Содержит требования к видео для различных экранов:

```json
{
  "commonSettings": {
    "fps": 25,
    "container": "mp4",
    "codec": "h264",
    "maxSize": 400,
    "allowedDurations": [15, 30]
  },
  "screens": [
    {
      "id": 1,
      "name": "Москва, межэтажка",
      "width": 8192,
      "height": 1056,
      "nightVersion": false
    },
    ...
  ]
}
```

**Примечание:** Backend проверяет только `codec` и `fps`. Проверка `resolution`, `duration`, `container` и `fileSize` выполняется на фронтенде.

---

## 🔒 Безопасность

Сервер включает следующие меры безопасности:

1. **Ограничение размера файла:** Максимум 400 МБ (соответствует лимиту фронтенда)
2. **Валидация MIME-типа:** Принимаются только файлы с `video/*`
3. **Защита от path traversal:** Проверка имени файла на подозрительные символы
4. **Временные файлы:** Использование `tempfile.NamedTemporaryFile` для безопасного хранения
5. **Автоочистка:** Временные файлы удаляются после обработки (даже при ошибках)
6. **CORS:** Настроен для разрешения cross-origin запросов

---

## 🐛 Troubleshooting

### Проблема: `ffprobe not found`

**Решение:** Установите FFmpeg (см. раздел "Установка FFmpeg" выше)

---

### Проблема: `ModuleNotFoundError: No module named 'fastapi'`

**Решение:** Установите зависимости:
```bash
pip install -r requirements.txt
```

---

### Проблема: `FileNotFoundError: screenRequirements.json`

**Решение:** Убедитесь, что файл `screenRequirements.json` находится в той же директории, что и `server.py`

---

### Проблема: Сервер не принимает большие файлы

**Решение:** Увеличьте лимит в `server.py`:
```python
MAX_FILE_SIZE = 1024 * 1024 * 1024  # 1 ГБ
```

---

### Проблема: CORS ошибки на фронтенде

**Решение:** CORS уже настроен в `server.py`. Если проблема сохраняется, проверьте логи сервера.

---

### Проблема: ngrok возвращает 403 Forbidden

**Решение:** Добавьте заголовок `ngrok-skip-browser-warning: true` в запросы:
```bash
curl -H "ngrok-skip-browser-warning: true" "https://your-ngrok-url/health"
```

---

## 📊 Логирование

Сервер логирует все важные события:
- ✅ Входящие запросы с именем файла и размером
- ✅ Результаты валидации (успех/ошибка)
- ✅ Ошибки с полным traceback
- ✅ Создание и удаление временных файлов

**Пример логов:**
```
2024-11-28 12:34:56 - __main__ - INFO - Получен запрос на валидацию видео: filename=test.mp4, content_type=video/mp4
2024-11-28 12:34:57 - __main__ - INFO - Файл сохранён во временное хранилище: /tmp/tmp123.mp4, размер: 52428800 bytes
2024-11-28 12:34:58 - __main__ - INFO - Извлечена информация о видео: codec=h264, fps=25.0
2024-11-28 12:34:58 - __main__ - INFO - ✅ Видео прошло валидацию: test.mp4
```

---

## 🧪 Тестирование

### Тест здоровья сервера
```bash
curl "http://localhost:8000/health"
```

### Тест валидации с валидным видео
```bash
curl -X POST "http://localhost:8000/api/validate-video" \
  -F "file=@valid_video.mp4"
```

### Тест валидации с невалидным видео
```bash
curl -X POST "http://localhost:8000/api/validate-video" \
  -F "file=@invalid_video.mp4"
```

### Тест получения требований
```bash
curl "http://localhost:8000/api/requirements"
```

---

## 🔧 Разработка

### Запуск в режиме разработки
```bash
uvicorn server:app --host 0.0.0.0 --port 8000 --reload
```

Флаг `--reload` автоматически перезапускает сервер при изменении кода.

### Просмотр документации API

После запуска сервера документация доступна по адресам:
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

---

## 🌐 Интеграция с фронтендом

### Пример запроса с фронтенда (JavaScript/React):

```javascript
const formData = new FormData();
formData.append('file', videoFile);

const response = await fetch('https://landless-serpentinely-amari.ngrok-free.dev/api/validate-video', {
  method: 'POST',
  body: formData,
  headers: {
    'ngrok-skip-browser-warning': 'true'
  }
});

const result = await response.json();

if (result.valid) {
  console.log('✅ Видео валидно');
} else {
  console.log('❌ Ошибки:', result.errors);
}
```

---

## 📝 Технические детали

### Поддерживаемые варианты H264 codec:
- `h264`
- `libx264`
- `avc1`
- `x264`

Все эти варианты автоматически нормализуются к `h264`.

### FPS с погрешностью:
- **Требуемый FPS:** 25
- **Допустимая погрешность:** ±0.1
- **Валидные значения:** 24.9 - 25.1

Примеры:
- ✅ 24.98 - валидно
- ✅ 25.00 - валидно
- ✅ 25.02 - валидно
- ❌ 24.8 - невалидно
- ❌ 30.0 - невалидно

---

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи сервера
2. Убедитесь, что FFmpeg установлен и доступен
3. Проверьте, что файл `screenRequirements.json` на месте
4. Проверьте размер файла (максимум 400 МБ)

---

## 📄 Лицензия

Проект создан для внутреннего использования.

---

## 🎯 Roadmap

- [ ] Добавить проверку resolution на бэкенде (если потребуется)
- [ ] Добавить проверку duration
- [ ] Добавить поддержку batch валидации (несколько файлов)
- [ ] Добавить unit тесты
- [ ] Добавить Docker контейнеризацию

---

**Версия:** 1.0.0  
**Дата создания:** Ноябрь 2024  
**Python:** 3.8+

